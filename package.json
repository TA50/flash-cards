{
  "name": "flash-cards",
  "packageManager": "yarn@3.2.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @flash-cards/ui start",
    "build": "yarn wsrun -eyt --report build "
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
